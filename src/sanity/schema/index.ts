import { subject } from './subject';
import { scene } from './scene';
import { siteSettings } from './siteSettings';

/** Every schema type in the media layer (ADR-0011). */
export const schemaTypes = [subject, scene, siteSettings];
